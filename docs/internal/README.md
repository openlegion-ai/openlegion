# Internal / Maintainer Ops

Docs for operating, debugging, and observing a *running* fleet — as opposed to the user/contributor guides in [`../README.md`](../README.md).

These are maintainer-facing. They may reference tooling and data files that live on the host / VPS (e.g. `data/*.db`, SSH-into-the-box workflows) rather than anything an end user touches.

## Docs

- [Session Observability](session-observability.md) — reconstruct any past human-rooted interaction (intent → action → outcome → cost); on-box / VPS debugging.
